runtime: "container"

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."

startCommand:
  type: "stdio"
  configSchema:
    type: "object"
    properties:
      config:
        type: "string"
        description: "Path to configuration file"
